# 作品集工作区

本仓库是用户（AI 交互设计方向求职）的作品集工作区，结构和排期见 `README.md`。Codex 在本仓库协作时遵循以下规则：

- 案例正文使用 Markdown 写作，定稿后才排版导出 PDF。修改文案时，直接编辑对应的 `cases/*/README.md` 或正文文件。
- **敏感信息永远不得进入本仓库**（公开仓库），包括薪资、可识别的在职公司信息和访谈对象个人信息。职业规划类内容应写入私有长期记忆库的 `profile/职业.md`。
- 案例②的 demo 代码放在 `cases/02-LLM座舱Agent/demo/`。
- 开始修改前先检查当前分支、工作区状态及相关文件，保留用户已有改动；完成后执行与改动相称的验证并清楚说明结果。

# 长期记忆库指针

用户的长期记忆库是独立私有仓库 **`jadecoltrane/memory`**（纯 Markdown + Obsidian 双链）。

- **开工时**：如果当前会话或工作区可以访问该仓库，先读取 `meta/index.md`，再按任务需要加载相关记忆。远程会话优先通过已授权的 GitHub 连接器访问；本地会话也可以使用已有 checkout。
- 如果长期记忆库不可访问，明确告知用户需要授权或提供访问方式；不得假装已读取或写入，也不得因此臆造记忆内容。
- **用户说“记下来”“存一下”“收藏这条”“写进记忆库”等触发词时**：这里的“记下来”始终指写入 `jadecoltrane/memory` Git 仓库，不是会话内短期记忆，也不是任何 AI 产品自带的记忆功能。先读取该仓库的 `AGENTS.md`；若不存在，则读取 `CLAUDE.md`，按其中规则提炼并写入，最后提交到其 `main` 分支。
- **收工时**：如果本次协作产生了值得长期沉淀的决定、踩坑或判断，并且记忆库可访问，则按其仓库规则写入；任何写入都应在最终回复中说明具体文件和提交结果。
