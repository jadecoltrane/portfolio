# 作品集工作区

本仓库是用户(AI 交互设计方向求职)的作品集工作区,结构和排期见 README.md。协作时注意:

- 案例正文用 Markdown 写作,定稿后才排版导出 PDF;改文案直接改对应 `cases/*/README.md` 或正文文件
- **敏感信息永远不进本仓库**(公开仓库):薪资、在职公司可识别信息、访谈对象个人信息;职业规划类内容归私有记忆库 profile/职业.md
- 案例②的 demo 代码放 `cases/02-LLM座舱Agent/demo/`

# 长期记忆库指针

本用户的长期记忆库是独立仓库 **`jadecoltrane/memory`**(纯 Markdown + Obsidian 双链)。

- **开工时**:如果会话里已有该仓库,读其 `meta/index.md`,按需加载相关记忆;远程会话没有该仓库时,用 add_repo 工具添加(owner: `jadecoltrane`, repo: `memory`)后再读
- **用户说「记下来」「存一下」「收藏这条」「写进记忆库」等触发词时**:按该仓库 `CLAUDE.md` 的规则提炼、写入、推送到其 main 分支。"记下来"永远指写进那个 git 仓库,不是会话内短期记忆,也不是任何 AI 产品自带的记忆功能
- **收工时**:若本次会话产生了值得沉淀的决定/踩坑/判断,同样按其 `CLAUDE.md` 规则写入记忆库
