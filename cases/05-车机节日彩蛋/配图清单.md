# 案例⑤ 配图清单（v3）

> 2026-08-27 按正文 v3 与 5 页分镜重列。旧清单（v1/v2 时期）已作废，原因见文末。

| # | 图 | 用在 | 内容要点 | 状态 |
|---|----|---|---------|------|
| 1 | 竞品对比矩阵 | P2 | 6 款车机 × 触发机制／视觉呈现／情感价值；下方收敛线：**6 products → 3 patterns → 1 opportunity** | 🟡 初稿在，需按 v3 的三种模式（完整闭环式／品牌人格式／探索游戏式）改收敛线 |
| 2 | 载体分配与相对层级 | P3 | 全屏联屏壁纸（仅壁纸桌面，底层）与边缘挂件（跨桌面，边缘层）**同为采用**；覆盖 Dock 排除；不出现内部层级编号 | 🟡 需改（现有图只画了层级堆叠，缺两个载体的落点差异） |
| **3** | **打断机制 · 原始二元分类** | **P4 左** | 打断逻辑／不打断逻辑两栏 + 打断原则；右上角标 `2025 · 当年交付物` | ✅ **完成** |
| **4** | **四级仲裁决策树** | **P4 右** | 系统事件 → 普通更新／注意力冲突／安全事件 → 继续／弱化・暂停／终止；底部标注每一级在原始二元分类里的位置；右上角标 `复盘产物 · 非当年交付物` | ✅ **完成** |

## 图 3 / 图 4 是一对，必须并置

两张同为 880 宽、同一套视觉语言，就是为 P4 左右并置画的。分开使用会丢掉整个案例的论点。

🔴 **两个角标绝不能删**：图 3 的「当年交付物」与图 4 的「复盘产物」。这一页的说服力全部来自诚实——一旦让人误以为四级模型是当年产出，案例价值归零。

配色语义（沿用 [排版规范.md](../../排版规范.md)）：终止 `#dc2626` · 弱化／暂停 `#b45309` · 继续灰 · Accent `#2563eb`。

## 废弃

| 原图 | 废弃原因 |
|---|---|
| 原图3 发现态／静默态对比 | 依据的"首次开机 2s 呼吸光效"经核对**无据**，正文已删 |
| 原图4 跨桌面状态流程 | 依据的"停止渲染／后台走完时间线／不续播"经核对**无据**，正文已删 |
| 原图5 生命周期流程图 | v3 的进入／退出逻辑已按原件重写（壁纸池、轮播开关、先提醒再渐隐），**需重画或弃用**，暂不排入 5 页 |

前两张已移入 `assets/case05/_废弃/`，仅作留痕，不进 PDF。

## 备注

- 全部脱敏重绘，不用内部文件；不出现品牌、车型、供应商、内部层级编号、内部版本号
- 风格与案例① 的图统一（同一套图例语言，W6 排版周统一微调）
- 现有 SVG 字体为 `PingFang SC, Noto Sans CJK SC, Microsoft YaHei`，排版周统一替换为正式中文字体
